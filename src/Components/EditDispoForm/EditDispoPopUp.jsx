/* eslint-disable react/prop-types */
import EditDispoForm from "./EditDispoForm";

const EditDispoPopUp = ({ dispo, setShowedit }) => {
  return <EditDispoForm dispo={dispo} setShowedit={setShowedit} />;
};

export default EditDispoPopUp;
